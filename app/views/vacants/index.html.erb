<% provide(:title, 'All Properties') %>

<h1>Vacants</h1>

<br>

<div class="row">
  <div class="text-center">
    <%= page_entries_info @vacants, model: 'location' %>
  </div>
  <hr>
</div>

<div class="row">

  <div class="col-sm-3">
    <!-- <br> -->
    <%= render 'vacants/advanced_search' %>
    <!-- <br> -->
  </div>

  <div class="col-sm-9" id="results-wrap">

    <%= render 'table_results' %>

    <div id="spinner" class="spinner col-sm-6 col-sm-offset-3 text-center">
      <!-- <%= image_tag 'ajax-loader.gif' %> -->
      <!-- <%= image_tag 'ring-alt.gif' %> -->
      <%= image_tag 'ripple.gif' %>
    </div>

    <!-- <table class="table table-hover">
      <thead>
        <tr>
    			<th>ID</th>
          <th>Address</th>
          <th>Block/Lot</th>
    			<th>Neighborhood</th>
    			<th>Police District</th>
    			<th>Council District</th>
        </tr>
      </thead>
      <tbody>
    		<% @vacants.each do |vacant| %>
    	    <tr>
    	      <th scope="row"><%= vacant.id %></th>
    	      <td>
    					<%= link_to vacant.fulladdress, vacant_path(id: vacant.id) %>
    				</td>
    	      <td><%= vacant.blocklot %></td>
    	      <td><%= vacant.neighborhood %></td>
    				<td><%= vacant.policedistrict %></td>
    				<td><%= vacant.councildistrict %></td>
    	    </tr>
    		<% end %>
      </tbody>
    </table> -->

    <!-- <hr>

    <div class="text-center">
      <%= will_paginate @vacants, previous_label: "&larr; Prev",
      next_label: "Next &rarr;", renderer: BootstrapPagination::Rails,
      style: "display: inline-block;", inner_window: 1, outer_window: 0 %>
    </div> -->

  </div>

</div>
